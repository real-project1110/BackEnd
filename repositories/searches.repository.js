const { User, Group, Comment, Post, GroupUser } = require('../models');
const { Op } = require('sequelize');

class SearchRepository {
  constructor() {
    super();
  }
}
module.exports = SearchRepository;
